server {
  listen 5173;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Optional reverse proxy (if you ever want to call backend via /api)
  location /api/ {
    proxy_pass http://wanderly-backend:8085;
  }
}
